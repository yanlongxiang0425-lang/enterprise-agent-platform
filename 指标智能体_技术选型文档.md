# 指标体系构建智能体 · 技术选型文档

> 版本：v1.0 · 日期：2026-05-14 · 适用范围：Dify 现有方案的下一代重构

---

## 1. 文档目的与背景

### 1.1 现状
当前指标体系构建智能体基于 **Dify Workflow** 实现，部署于 `aigc.midea.com/dify2`，应用 ID `35b561f1-db2a-4676-8b61-2b5604731400`。核心链路：

```
开始 → 文件与JSON判断 → 文档提取 → 字符串扁平化
     → 业务分类(LLM) → 条件分支
     → (单域: 列表操作 + 指标推导 AGENT)
     → (多域: 并行迭代 + 指标推导 Agent)
     → Unicode 转义 → 聚合结果 → 结束
```

主链路使用 **Qwen3-235B-A22B (CHAT)** 模型，Agent 策略为 **ReAct**，工具包含 **BOCHA_SEARCH（博查 Web 搜索）** 和 **领域体系构建 RAG 检索**。

### 1.2 痛点
1. 全部链路在 Dify 上拖拽实现，**工程化与版本管理薄弱**，难以做单元测试与回归
2. 多个 Python 代码节点处理"脏活"（Unicode 转义、字符串扁平化、JSON 聚合），逻辑散落且无法复用
3. 单 Agent + 235B 巨型模型，**单次 Token 成本与延迟过高**
4. 没有缓存、可观测、评测闭环
5. 业务流程图中"重新评估可行性或加入人为确认"环节尚未落地
6. **大 JSON 输入瓶颈**：上千上万张表的 schema 拼成一个 JSON，超出模型上下文窗口，且注意力稀释导致分类/推导准确率下降
7. 知识库默认 pgvector，召回与性能尚未做过严肃评估
8. 知识库（Excel/Word）更新流程不规范，缺自动化 pipeline

### 1.3 改造目标
- 从 Dify 迁出，自建可控的 Python AI 服务
- 复用现有 PG 向量库（如评估满足）
- 引入模型分流策略降本提速
- 解决大 JSON 输入瓶颈
- 提供离线部署能力（内网无外网环境）
- 补齐审计、脱敏、可观测、人工确认

---

## 2. 总体技术路线

### 2.1 核心决策
| 决策项 | 选择 | 关键理由 |
|---|---|---|
| 主语言 | **Python** | AI 生态（LangChain / vLLM / Embedding 等）首发支持；公司 Java 主栈保留在业务接入层 |
| Agent 编排 | **LangGraph 1.0+** | 状态机式编排，可调试、可重放；告别 Dify 不可控的拖拽 |
| LLM 推理引擎 | **vLLM 0.6+** | 国内高吞吐 SOTA，支持 AWQ/GPTQ 量化、guided_json 结构化输出 |
| 模型策略 | **三级分流** | 7B 做分类/路由，32B-AWQ 做推导，Embedding/Rerank 用 BGE 系列 |
| 向量库 | **PostgreSQL 16 + pgvector 0.7.4** | 知识库 < 5 万 chunk，HNSW 性能足够；与业务库同实例 |
| 模型采购路径 | **路径 A**：前期 API 验证 → 确定模型后采购 GPU | CPU 也可跑通功能链路（不追性能） |
| 部署形态 | 单机虚拟机 + Docker Compose；**全离线包** | 内网无外网，必须自带 wheel/镜像/模型 |

### 2.2 模型分流策略

```
┌──────────────┬─────────────────────────────────┬──────────────┐
│ 任务等级     │ 模型                             │ 调用场景      │
├──────────────┼─────────────────────────────────┼──────────────┤
│ 轻量 (L1)    │ Qwen2.5-7B-Instruct             │ 业务域分类、 │
│              │ 或 Qwen2.5-3B-Instruct          │ 意图识别、   │
│              │                                 │ Query 改写   │
├──────────────┼─────────────────────────────────┼──────────────┤
│ 重型 (L2)    │ Qwen2.5-32B-Instruct-AWQ        │ 指标推导     │
│              │ 或 Qwen3-30B-A3B (MoE 激活3B)   │ Agent 主链   │
├──────────────┼─────────────────────────────────┼──────────────┤
│ Embedding    │ bge-m3 (560M)                   │ 知识库向量化 │
│              │                                 │ + 检索       │
├──────────────┼─────────────────────────────────┼──────────────┤
│ Rerank       │ bge-reranker-v2-m3 (568M)       │ 检索结果重排 │
└──────────────┴─────────────────────────────────┴──────────────┘
```

**为什么不用 Qwen3-235B-A22B？**
- 235B 参数即使是 MoE 激活 22B，对单机部署而言显存仍然吃紧（约 140GB FP8 / 200GB FP16）
- 单 Token 推理延迟高，QPS 100 场景下需要多卡集群
- 实测表明 32B 模型 + 良好的 Prompt + RAG 已能覆盖企业指标推导 90% 场景
- 等 GPU 采购后，如果 32B 不达标再升级到 72B-AWQ 即可

### 2.3 大 JSON 瓶颈解决方案

| 层 | 手段 | 效果 |
|---|---|---|
| L1 Schema 瘦身 | 只保留 `table / column / comment / dtype / sample`，去除冗余字段 | 单表 schema 从 ~1.5KB 压到 ~200B |
| L2 两阶段 Map-Reduce | ① 7B 模型按 50 张表/批做业务域分类 ② 32B 模型按域做指标推导 | 单次输入 ≤ 10KB（远小于上下文窗口） |
| L3 结构化输出约束 | vLLM `guided_json` + JSON Schema，**Outlines / instructor** 兜底 | 杜绝模型输出"长篇散文式" JSON 失败 |
| L4 流式 + SSE | 推导结果按业务域分块流式返回，前端渐进渲染 | 用户首字时间 P95 ≤ 3s |
| L5 缓存 + 增量 | 表 schema MD5 作为 cache key；命中跳过 LLM | 重复建模 QPS 提升 10× |

---

## 3. 组件清单（详版）

### 3.1 编排与 Agent 层

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **LangChain** | **1.0.5+** | 工具/模型抽象 | 1.0 是 GA 版本，API 大幅稳定；不再使用 0.x |
| **LangGraph** | **1.0.3+** | 多步骤状态机编排 | 替代 Dify 工作流，支持中断/恢复/重放 |
| **LangSmith Self-Hosted** | 0.3.x | LLM Trace（可选） | 也可用 Arize Phoenix 替代（更友好离线） |
| **Pydantic** | 2.9.2 | 数据校验/Schema 定义 | 与 FastAPI / LangChain 1.0 强绑定 |
| **instructor** | 1.6.4 | LLM 结构化输出 | 兜底 vLLM guided_json |
| **Outlines** | 0.1.7 | 受限文法生成 | 复杂 JSON Schema 场景 |

### 3.2 LLM 推理与模型

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **vLLM** | 0.6.3.post1 | 模型推理引擎 | GPU 期使用；支持 AWQ/GPTQ/FP8 |
| **llama.cpp / llama-cpp-python** | 0.3.1 | CPU 推理引擎 | **前期 CPU 验证用**，加载 GGUF 量化模型 |
| **vllm-openai 容器镜像** | v0.6.3.post1 | OpenAI 兼容 API | 与 LangChain 无缝对接 |
| **Qwen2.5-7B-Instruct** | 官方 HF | 轻量分流 | GGUF Q4_K_M ≈ 4.5GB，CPU 可跑 |
| **Qwen2.5-32B-Instruct-AWQ** | 官方 HF | 主推导 | INT4 约 18GB 显存，单卡 A100/A30 可跑 |
| **bge-m3** | 1.5 (BAAI) | Embedding | 1024 维，dense + sparse + ColBERT 三合一 |
| **bge-reranker-v2-m3** | (BAAI) | Rerank | 重排 top-50 → top-5 |

### 3.3 RAG 与文档解析

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **LlamaIndex** | 0.12.x | RAG 框架（可选） | 也可自建轻量 RAG 不引入此依赖 |
| **unstructured** | 0.15.13 | 统一文档解析 | 支持 docx/xlsx/pdf/html |
| **python-docx** | 1.1.2 | Word 解析 | 章节级切分 |
| **openpyxl** | 3.1.5 | Excel 解析 | 字段级 + 单元格批注 |
| **pandas** | 2.2.3 | 表格处理 | Excel → DataFrame |
| **pypdf** | 5.1.0 | PDF 解析 | 兜底 PDF 资料 |
| **tiktoken** | 0.8.0 | Token 计数 | Prompt 切片估算 |
| **FlagEmbedding** | 1.3.x | BGE 模型本地加载 | sentence-transformers 备选 |

### 3.4 向量库与数据库

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **PostgreSQL** | 16.4 | 业务库 + 向量库 | 同实例不同 schema |
| **pgvector** | 0.7.4 | 向量扩展 | 启用 HNSW，`m=16, ef_construction=64` |
| **pg_jieba** 或 **zhparser** | 最新 | 中文全文检索 | 与向量召回做混合检索 |
| **Redis** | 7.4.1 | 缓存 + Celery Broker | LRU 策略，单实例 16GB |
| **MinIO** | RELEASE.2024-10 | 对象存储 | 上传文件落地（虚机可挂载磁盘代替） |

### 3.5 Web 与服务层

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **FastAPI** | 0.115.4 | API 框架 | 与 Pydantic v2 强一致 |
| **Uvicorn** | 0.32.0 | ASGI Server | `--workers N` 多进程 |
| **gunicorn** | 23.0.0 | 进程管理 | 生产部署 + UvicornWorker |
| **httpx** | 0.27.2 | HTTP 客户端 | 替代 requests，异步原生 |
| **SQLAlchemy** | 2.0.36 | ORM | 异步引擎 `asyncpg` |
| **asyncpg** | 0.30.0 | PG 异步驱动 | 高性能 |
| **alembic** | 1.13.3 | DB 迁移 | DDL 版本化 |

### 3.6 任务编排与异步

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **Celery** | 5.4.0 | 异步任务队列 | 业务域并行推导 |
| **flower** | 2.0.1 | Celery 监控 | 内网可用 |
| **APScheduler** | 3.10.4 | 定时任务 | 知识库增量更新调度 |

### 3.7 网关与限流

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **APISIX** | 3.10.0 | API 网关 | 限流、鉴权、灰度（推荐） |
| **Nginx** | 1.26.2 | 反向代理（备选） | 单机部署轻量替代 |
| **etcd** | 3.5.16 | APISIX 配置中心 | 内嵌即可 |

### 3.8 审计与脱敏

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **Presidio Analyzer** | 2.2.355 | PII 识别 | 手机号、身份证、邮箱、银行卡 |
| **Presidio Anonymizer** | 2.2.355 | PII 脱敏 | 支持 mask/replace/hash |
| **Loguru** | 0.7.2 | 日志库 | 结构化 JSON 日志 |
| **OpenPolicyAgent (OPA)** | 0.69.0 | 策略引擎（可选） | 复杂权限规则 |

### 3.9 可观测

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **OpenTelemetry SDK** | 1.27.0 | Trace/Metrics/Logs 采集 | Python instrumentation |
| **Prometheus** | 2.55.0 | Metrics 存储 | 30 天保留 |
| **Grafana** | 11.3.0 | 可视化 | 内置 OTel/Prom 数据源 |
| **Arize Phoenix** | 5.x | LLM Trace（开源、离线） | 替代 LangSmith |
| **Loki** | 3.2.0 | 日志聚合（可选） | 与 Grafana 集成 |

### 3.10 部署与离线

| 组件 | 版本 | 角色 | 备注 |
|---|---|---|---|
| **Docker** | 27.3.x | 容器运行时 | 离线 tar 镜像分发 |
| **docker-compose** | 2.29.x | 编排 | 单机首选 |
| **pip + devpi-server** | 6.14 | 离线 Python 仓库 | `pip download` 打包 wheel |
| **harbor** (可选) | 2.11.1 | 镜像仓库 | 多机时使用 |

---

## 4. Java vs Python 详细对比

### 4.1 维度对比表

| 维度 | Python | Java | 胜方 |
|---|---|---|---|
| **AI 生态** | LangChain / LangGraph / LlamaIndex / vLLM 等几乎所有 SOTA 框架首发 | LangChain4j / Spring AI 滞后 1-3 个月，特性不全 | **Python ★★★** |
| **模型推理** | vLLM 原生 Python API，performant，可直接嵌入 | 必须通过 HTTP/gRPC 调用 vLLM，多一跳 | Python |
| **结构化输出** | instructor / Outlines / Pydantic 联动天然 | LangChain4j 有 `@StructuredOutput`，但成熟度差距 | Python |
| **文档解析** | unstructured / python-docx / openpyxl 一应俱全 | Apache POI / Tika 也成熟，但 LLM 链路整合需自写胶水 | 持平 |
| **向量库 SDK** | pgvector-python / Milvus / Qdrant SDK 第一时间发布 | pgvector-java / Milvus Java 通常滞后 | Python |
| **并发模型** | asyncio + 多进程；GIL 限制 CPU 密集 | JVM 线程池/Reactor，高并发友好 | **Java ★★★** |
| **内存/性能** | 较高，需 jemalloc 调优 | JVM 长跑稳定 | Java |
| **类型/工程化** | Pydantic 2 弥补类型；依赖管理 pip/uv 略乱 | Maven/Gradle 成熟，类型严格 | Java |
| **团队习惯** | 公司 AI 同学需具备 Python 能力 | 公司主栈 Java，已有积累 | Java |
| **离线部署** | wheel 离线包成熟（manylinux）；模型 HF mirror | jar 包天然离线；但 PyTorch 依赖仍需 Python | Python（含模型时） |
| **可观测** | OpenTelemetry 完善；LangSmith / Phoenix 一线集成 | OpenTelemetry Java agent 完善；LLM trace 弱 | Python |
| **社区活跃度** | LLM 项目 PR/release 频率极高 | LLM 项目活跃度约为 Python 1/5 | **Python ★★★** |

### 4.2 推荐结论

**核心 AI 服务用 Python，业务接入层 / 管理后台保留 Java**，通过 HTTP/gRPC 解耦。

**拆分理由**：
1. **AI 服务（Python）**：指标推导、RAG 检索、业务域分类、Agent 编排——这部分 Python 生态远胜 Java，且模型/Embedding/Rerank 几乎全在 Python 侧
2. **业务接入层（Java）**：用户管理、权限、租户、审计查询、报表导出、与 ERP/数仓对接——公司 Java 团队继续负责，复用既有 Spring Boot 体系
3. **解耦方式**：Python AI 服务暴露 OpenAPI；Java 通过 OpenFeign/WebClient 调用

### 4.3 风险与缓解

| 风险 | 缓解 |
|---|---|
| Java 团队学习 Python | 划定 1~2 人专项；提供 Python 基线项目模板；2 周培训计划 |
| Python GIL 限制并发 | gunicorn `--workers N` 多进程 + asyncio；CPU 密集用 Celery |
| 双栈运维复杂 | 统一 Docker Compose；日志规范统一 JSON；可观测一套 OpenTelemetry |
| 框架升级带兼容性 | 锁定 `requirements.txt` + 离线 wheel 仓；变更走 CI/CD |

---

## 5. PG 向量库适配评估

### 5.1 知识库规模评估

| 指标 | 当前值 | 评估 |
|---|---|---|
| 文档总数 | < 1000（Excel + Word） | 小 |
| 字段总数 | 顶多几万设计字段 | 小 |
| 预估 chunk 数 | < 5 万（按 200 字一个 chunk） | 小 |
| 向量维度 | bge-m3 = 1024 | 标准 |
| 总向量空间 | 5 万 × 1024 × 4B ≈ **200MB** | 小 |
| 检索并发 QPS | 100 | 中 |

### 5.2 pgvector 性能基准（公开数据 + 推算）

| 索引类型 | 召回率 @ 10w 向量 | QPS（单机 8 核） |
|---|---|---|
| 暴力（无索引） | 100% | < 10 |
| IVFFlat | 95% | ~ 200 |
| **HNSW（推荐）** | **99%** | **~ 1200** |

**结论：pgvector HNSW 在 5 万向量规模下，单机 QPS 远超 100，满足要求且无需更换。**

### 5.3 推荐索引参数

```sql
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE kb_chunks (
  id BIGSERIAL PRIMARY KEY,
  doc_id VARCHAR(64) NOT NULL,
  domain VARCHAR(64),
  content TEXT NOT NULL,
  embedding vector(1024) NOT NULL,
  metadata JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- HNSW 索引（适合中小规模，构建快，查询准）
CREATE INDEX idx_kb_chunks_embedding_hnsw
  ON kb_chunks USING hnsw (embedding vector_cosine_ops)
  WITH (m = 16, ef_construction = 64);

-- 业务域过滤索引
CREATE INDEX idx_kb_chunks_domain ON kb_chunks(domain);

-- 全文检索（混合召回）
CREATE INDEX idx_kb_chunks_content_fts
  ON kb_chunks USING gin(to_tsvector('jiebacfg', content));
```

### 5.4 何时该换 Milvus / Qdrant？

| 触发条件 | 替换方案 |
|---|---|
| 向量数 > 500 万 | Milvus 2.4 / Qdrant 1.12 |
| 多租户隔离需求强 | Milvus（Collection 级别） |
| 需要稀疏 + 稠密混合索引（BM25+Dense） | Qdrant / Milvus 2.4 |
| 需要二级索引、复杂过滤 | Qdrant |

**当前 5 万 chunk 远不到阈值，至少未来 2 年内 pgvector 都够用。**

---

## 6. 最低环境配置（重点新增章节）

### 6.1 三阶段部署策略

| 阶段 | 用途 | 周期 | 推理形态 |
|---|---|---|---|
| **阶段 1：CPU 验证期** | 跑通功能链路、回归测试 | 0~2 周 | llama.cpp + GGUF Q4 |
| **阶段 2：API 验证期** | 性能基线、模型选型对比 | 2~6 周 | 租用云 LLM API（如阿里百炼、火山引擎方舟） |
| **阶段 3：GPU 生产期** | 上线服务 | 6 周后 | 自建 vLLM 集群 |

### 6.2 阶段 1：CPU 验证期（最低配置）

> 目标：**只验证功能正确性，不追性能**。可在开发机或低配虚机上运行。

| 服务 | CPU | 内存 | 磁盘 | 备注 |
|---|---|---|---|---|
| 应用服务（FastAPI + LangGraph） | 4 核 | 8 GB | 50 GB | 全部 Python 业务逻辑 |
| PostgreSQL + pgvector | 2 核 | 4 GB | 100 GB SSD | 业务 + 向量 |
| Redis | 1 核 | 2 GB | 10 GB | Celery + 缓存 |
| Celery Worker × 2 | 2 核 | 4 GB | 10 GB | 异步推导 |
| llama.cpp（7B Q4） | 8 核 | 8 GB | 20 GB | 单次 ≈ 3-8 token/s（够功能验证） |
| llama.cpp（32B Q4，可选） | 16 核 | 24 GB | 40 GB | 单次 ≈ 0.5-1.5 token/s（慢但可跑） |
| bge-m3 + reranker（CPU） | 4 核 | 4 GB | 6 GB | ONNX Runtime 加速 |
| Prometheus + Grafana | 2 核 | 4 GB | 50 GB | 监控 |
| **合计（最小）** | **24 核** | **40 GB** | **300 GB** | 单台 32C/64G 虚机即可 |

**关键说明**：
- 7B 模型 Q4_K_M GGUF 文件约 4.5GB，跑分类完全够；32B Q4_K_M 约 19GB
- 实际推理延迟 32B 模型在 16C CPU 下约 30~120s/请求，**只做功能验证别上量**
- Embedding 用 bge-m3 ONNX 版（FastEmbed 库）能跑到 100 ms/请求量级

### 6.3 阶段 2：API 验证期

> 目标：用云端 API 跑性能基线，确定后续要采购什么 GPU。

| 服务 | 配置 | 备注 |
|---|---|---|
| 应用服务 | 同阶段 1 | 不变 |
| 模型 API | 阿里百炼 / 火山方舟 / DeepSeek API | 临时联网开口（运维白名单） |
| Token 预算 | 50~100 元/天 × 4 周 ≈ 2000~4000 元 | 用于 A/B 跑模型对比 |
| 配套 | Phoenix（LLM Trace） | 记录每次调用，便于回归 |

**对比矩阵**（建议跑的 5 个候选）：

| 模型 | 上下文 | 单价（元/百万 token） | 用途 |
|---|---|---|---|
| Qwen2.5-7B-Instruct | 32K | ~0.5 | 分流基线 |
| Qwen2.5-14B-Instruct | 32K | ~2 | 中等参数对照 |
| Qwen2.5-32B-Instruct | 32K | ~4 | 推荐主力 |
| Qwen2.5-72B-Instruct | 32K | ~8 | 上限对照 |
| DeepSeek-V2.5 | 64K | ~2 | 长上下文对照 |

跑完后选择"在指标推导准确率上达到验收阈值且单价最低"的模型组合。

### 6.4 阶段 3：GPU 生产期（推荐最低 GPU 配置）

| 角色 | GPU 配置（推荐） | 备选 | 备注 |
|---|---|---|---|
| **主推导（32B-AWQ）** | 1 × A100 40GB（或 A30 24GB ×2） | 1 × L20 48GB（性价比高） | INT4 量化后 ~18GB，留余量 |
| **轻量分流（7B）** | 与主推导共卡 / 1 × T4 16GB | 1 × RTX 4090 24GB | 7B FP16 约 14GB |
| **Embedding + Rerank** | 1 × T4 16GB（或 CPU 凑合） | / | 吞吐够即可 |

**单机推荐配置（最低）**：

| 项 | 规格 |
|---|---|
| CPU | 2 × Intel Xeon Gold 6338（32 核 / 64 线程） |
| 内存 | 256 GB DDR4 ECC |
| GPU | 1 × A100 40GB **或** 1 × L20 48GB |
| 系统盘 | 480 GB NVMe SSD |
| 数据盘 | 2 TB NVMe SSD（模型 + 向量库） |
| 网络 | 双口万兆 |
| 操作系统 | Ubuntu 22.04 LTS / CentOS Stream 9 |
| CUDA | 12.4 |
| Driver | 550.x |

> 如果未来 QPS > 200 或需要 72B，则推荐 **2 × A100 80GB** 或 **1 × H20 96GB**。

### 6.5 离线安装包清单

> 内网无外网，必须打全离线包。下表列出所有需要预先下载的 artifact。

| 类型 | 内容 | 体积估算 | 下载源 |
|---|---|---|---|
| Docker 镜像 | postgres:16.4-bookworm、redis:7.4-bookworm、vllm/vllm-openai:v0.6.3、apisix:3.10、grafana/grafana:11.3、prom/prometheus:v2.55、arizephoenix/phoenix:5 | ≈ 15 GB | 在联网机用 `docker save` 打 tar |
| Python wheel | 全 `requirements.txt` 的 wheel | ≈ 3 GB | `pip download -d ./wheels -r requirements.txt --platform manylinux2014_x86_64` |
| 系统包（如需） | gcc / postgresql-client / libgomp | ≈ 200 MB | apt/yum 离线源 |
| 模型权重 | Qwen2.5-7B-Instruct (GGUF Q4)、Qwen2.5-32B-Instruct-AWQ、bge-m3、bge-reranker-v2-m3 | ≈ 50 GB | HuggingFace / ModelScope |
| Node.js 包（如需前端） | npm cache 包 | ≈ 1 GB | `npm pack` 或 verdaccio |
| 文档 | 离线版 API 文档（FastAPI 自带 Swagger） | ≈ 50 MB | 启动后自带 |
| **合计** | | **≈ 70 GB** | 用 1 块 128GB U 盘可拷入 |

### 6.6 网络与端口规划

| 服务 | 端口 | 协议 | 说明 |
|---|---|---|---|
| API 网关 | 8080 | HTTP | 对外入口 |
| FastAPI（多进程） | 8000~8007 | HTTP | 网关下游 |
| PostgreSQL | 5432 | TCP | 仅内网 |
| Redis | 6379 | TCP | 仅内网 |
| vLLM | 8001 | HTTP | OpenAI 兼容 |
| Prometheus | 9090 | HTTP | 监控 |
| Grafana | 3000 | HTTP | 可视化 |
| Phoenix | 6006 | HTTP | LLM Trace |
| Celery Flower | 5555 | HTTP | 任务监控 |

---

## 7. requirements.txt 完整版

```text
# ============ Core Web ============
fastapi==0.115.4
uvicorn[standard]==0.32.0
gunicorn==23.0.0
pydantic==2.9.2
pydantic-settings==2.6.1
httpx==0.27.2
python-multipart==0.0.17

# ============ LangChain Ecosystem (1.0+) ============
langchain==1.0.5
langchain-core==1.0.5
langchain-community==1.0.3
langchain-openai==1.0.2
langgraph==1.0.3
langgraph-checkpoint==1.0.5
langsmith==0.3.1

# ============ Structured Output ============
instructor==1.6.4
outlines==0.1.7
tiktoken==0.8.0

# ============ LLM Inference (offline) ============
llama-cpp-python==0.3.1            # CPU 阶段
vllm==0.6.3.post1                   # GPU 阶段（在 GPU 机部署）

# ============ Embedding / Rerank ============
FlagEmbedding==1.3.4
sentence-transformers==3.3.1
fastembed==0.4.2                    # ONNX 加速（CPU 友好）
transformers==4.46.2
torch==2.4.1                        # CPU 版用 torch+cpu

# ============ Document Parsing ============
unstructured[xlsx,docx]==0.15.13
python-docx==1.1.2
openpyxl==3.1.5
pandas==2.2.3
pypdf==5.1.0

# ============ Database ============
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.13.3

# ============ Cache & Queue ============
redis==5.2.0
celery[redis]==5.4.0
flower==2.0.1
apscheduler==3.10.4

# ============ Audit & Security ============
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ============ Observability ============
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-sqlalchemy==0.48b0
opentelemetry-exporter-otlp==1.27.0
arize-phoenix==5.10.0
loguru==0.7.2
prometheus-client==0.21.0

# ============ Utilities ============
tenacity==9.0.0
orjson==3.10.11
typer==0.13.0
rich==13.9.4
```

---

## 8. docker-compose.yml 骨架（部署参考）

```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16.4-bookworm
    environment:
      POSTGRES_DB: metric_agent
      POSTGRES_USER: ma
      POSTGRES_PASSWORD: ${PG_PASSWORD}
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./init-pgvector.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"

  redis:
    image: redis:7.4-bookworm
    command: redis-server --maxmemory 12gb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"

  api:
    image: metric-agent-api:1.0.0
    depends_on: [postgres, redis]
    environment:
      DATABASE_URL: postgresql+asyncpg://ma:${PG_PASSWORD}@postgres:5432/metric_agent
      REDIS_URL: redis://redis:6379/0
      LLM_ROUTER_MODE: api   # 阶段 2：api / 阶段 3：vllm
      OTEL_EXPORTER_OTLP_ENDPOINT: http://phoenix:6006
    deploy:
      replicas: 4
    ports:
      - "8000:8000"

  celery-worker:
    image: metric-agent-api:1.0.0
    command: celery -A app.celery_app worker -l info -c 4
    depends_on: [postgres, redis]
    deploy:
      replicas: 2

  vllm:
    image: vllm/vllm-openai:v0.6.3.post1
    runtime: nvidia
    profiles: ["gpu"]               # 阶段 1 不启用
    command: >
      --model /models/Qwen2.5-32B-Instruct-AWQ
      --quantization awq
      --max-model-len 32768
      --gpu-memory-utilization 0.9
      --guided-decoding-backend xgrammar
    volumes:
      - ./models:/models
    ports:
      - "8001:8000"

  phoenix:
    image: arizephoenix/phoenix:5.10
    ports:
      - "6006:6006"

  prometheus:
    image: prom/prometheus:v2.55.0
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prom_data:/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:11.3.0
    volumes:
      - grafana_data:/var/lib/grafana
    ports:
      - "3000:3000"

volumes:
  pg_data:
  redis_data:
  prom_data:
  grafana_data:
```

---

## 9. 选型决策一页纸

| 关注点 | 当前 Dify 方案 | 新方案 | 收益 |
|---|---|---|---|
| 编排 | Dify 拖拽 | LangGraph 1.0 状态机 | 可测试、可版本化、可重放 |
| 模型 | 单一 Qwen3-235B | 7B+32B 分流 | 单次成本 ↓ 60%+，延迟 ↓ 40% |
| 大 JSON | 一次塞入 | 5 层瘦身/切片/Map-Reduce/流式/缓存 | 突破上下文窗口；准确率 ↑ |
| 向量库 | pgvector（沿用） | pgvector + HNSW + 混合检索 | 不换轮子，仅调优 |
| 部署 | Dify SaaS | Docker Compose 离线包 | 内网可控 |
| 审计/脱敏 | 无 | Presidio + Loguru + 审计 schema | 合规达标 |
| 可观测 | 弱 | OTel + Phoenix + Grafana | 问题定位分钟级 |
| 团队语言 | 业务 Java + Dify 拖拽 | Java 接入层 + Python AI 服务（HTTP 解耦） | 各司其职 |
| 模型采购 | / | A 路径：API 验证 → 选型 → 采购 GPU | 风险最低 |

---

## 10. 验收清单

- [ ] CPU 阶段：完整跑通"上传 Excel → 业务域分类 → 指标推导 → 输出 metric_system JSON"全链路
- [ ] API 阶段：在 5 个候选模型上完成准确率/延迟/成本三维评估，输出选型报告
- [ ] GPU 阶段：vLLM 部署 + 32B-AWQ 加载 + guided_json 验证；QPS 100 下 P95 ≤ 5s
- [ ] 知识库：5 万 chunk 入库；混合召回 Recall@10 ≥ 90%
- [ ] 大 JSON：1000 张表输入下，分类/推导链路稳定运行，无 OOM 与 Token 超限
- [ ] 审计：所有 LLM 调用落审计表；PII 字段自动脱敏
- [ ] 可观测：Grafana 看板 + Phoenix Trace + 告警规则齐全
- [ ] 离线：所有依赖打入离线包，可在白名单虚机一键起服

---

*文档结束。任何条目可单独迭代深化。*
